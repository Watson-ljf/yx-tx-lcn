server:
  port: 8083

yx:
  nacos:
    server-addr: 127.0.0.1:8848

spring:
  application:
    name: yx-spring-boot-admin-monitor
  cloud:
    nacos:
      discovery:
        server-addr: ${yx.nacos.server-addr}