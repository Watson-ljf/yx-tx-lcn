server:
  port: 8081

yx:
  nacos:
    server-addr: 127.0.0.1:8848

spring:
  application:
    name: yx-demo-spring-service-a
  cloud:
    nacos:
      discovery:
        server-addr: ${yx.nacos.server-addr}